apiVersion: v1
kind: Pod
metadata:
  name: "{{ include "koku-chart.fullname" . }}-test-connection"
  labels:
    {{- include "koku-chart.labels" . | nindent 4 }}
  annotations:
    "helm.sh/hook": test
spec:
  containers:
    - name: wget
      image: busybox
      command: ['wget']
      args: ['{{ include "koku-chart.fullname" . }}:{{ .Values.service.port }}']
  restartPolicy: Never
